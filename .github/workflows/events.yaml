name: Push Events
on: 
  push:
    branches:
      - main
      - test
jobs:
  show-info:
      runs-on: ubuntu-latest
      steps:
      - name: info
        run: |
          echo "This is the first workflow"
          echo "Hello world"
