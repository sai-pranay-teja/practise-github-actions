name: Push Events
on:
  schedule:
    - cron: "*/5 * * * *"
jobs:
  show-info:
      runs-on: ubuntu-latest
      steps:
      - name: info
        run: |
          echo "This is the first workflow"
          echo "Hello world"
